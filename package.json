{
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "npm-run-all": "^4.1.5"
  },
  "scripts": {
  "dev": "vite",
  "build": "vite build",
  "preview": "vite preview",
  "predeploy": "npm run build",
  "deploy": "gh-pages -d dist"
}

}
